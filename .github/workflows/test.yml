name: test

on:
  push:
    branches: [main]

jobs:
  fractal_build:
    name: Fractal Build
    # runs-on: ubuntu-latest
    runs-on: ubuntu-18.04
    steps:
      - name: setup Node
        uses: actions/setup-node@v1
        with:
          node-version: 12.16.1
      - name: install
        run: npm install
      - name: build
        run: fractal build
        # run: |
        #   npm install
        #   fractal build
